name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov

    # Add python dependencies
  - numpy

    # Pip-only installs
  - pip:
    - codecov
